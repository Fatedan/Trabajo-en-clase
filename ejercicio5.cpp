#include <iostream>
#include <math.h>

int main(){
    
    int M,N,k,i=1;
    for (M=1;M<=1000;M++)
        {for (N=1;N<=1000;N++)
            {k=2*pow(M,3)+3*pow(N,2);
            {if (((N % 2)==1) && ((M % 2)==1)) 
                {if(k<540)
                    {printf("\n (%i) (%i,%i) \n" ,i,M,N );
                    i++;}
                }
            }
            }
        
        }
// 








}